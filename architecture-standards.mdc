---
description: 架构与设计原则——分层、边界、演进、SOLID/YAGNI（结构层）
alwaysApply: true
---

# 高级架构规则

## 设计原则

- SOLID、DRY、KISS：优先可读性，避免不必要抽象与过早优化
- 优先组合而非继承；方法单一职责、类高内聚
- 始终考虑：可维护性、可测试性、可扩展性、性能、安全性

## 分层与职责

- **单一职责**：每个模块/类只做一件事；业务逻辑、数据访问、展示/入口层分离
- **依赖方向**：高层依赖抽象，不依赖具体实现（面向接口编程）
- **边界清晰**：跨层调用走明确定义的 API，禁止跨层「抄近路」直接访问底层细节

## 模块设计

- **高内聚、低耦合**：模块内部紧密相关，模块间通过稳定接口通信
- **开闭原则**：对扩展开放，对修改关闭；新行为优先通过新增而非修改核心逻辑实现
- **配置外置**：环境相关参数（URL、密钥、开关）不进代码硬编码

## 并发与分布式（JUC/后端通用）

- 共享可变状态必须明确同步策略（锁、CAS、不可变对象、线程封闭）
- 线程池显式配置核心参数，禁止 `Executors` 快捷工厂（阿里规范）
- 异步操作需定义：超时、取消、失败回调、资源释放
- 分布式场景考虑：幂等、重试退避、最终一致、链路追踪

## 演进与兼容

- 公共 API 变更需考虑向后兼容；破坏性变更须显式标注并给出迁移路径
- 废弃（@Deprecated）留过渡期，不 silent break
- 大重构分步交付：每步可独立编译、测试、回滚

## 反模式（禁止）

- God Class / 万能 Service 包揽所有逻辑
- 循环依赖、双向耦合
- 在 Domain 层引入框架注解或基础设施细节
- 为「可能用到」提前建抽象层（YAGNI 违反）

## 架构决策记录

涉及以下情况时，实现前应简要说明选型理由（默认口头说明，用户明确要求时再写文档）：

- 引入新依赖或中间件
- 改变模块边界或数据流
- 选用特定并发/缓存/持久化策略
