---
description: 编码前思考流程——目标澄清、方案评估、风险识别（过程层）
alwaysApply: true
---

# 编码前思考规则

在写任何实现代码之前，必须先完成以下思考（复杂任务可简短输出，简单任务内化执行）：

## 1. 理解目标

- 用户真正要解决的问题是什么？成功标准是什么？
- 这是新功能、bug 修复、重构还是探索性问题？
- 有无隐含约束：兼容性、性能、安全、时间、不可改动的模块？

## 2. 定位上下文

- 相关代码在哪里？现有模式是什么？
- 有没有可复用的抽象、工具类、测试夹具？
- 改动会影响哪些调用方、配置、部署或数据？

## 3. 方案对比（多方案时）

| 维度 | 需评估 |
|------|--------|
| 正确性 | 是否覆盖边界与并发/异常场景 |
| 复杂度 | 是否为最小可行方案 |
| 一致性 | 是否符合项目现有架构与风格 |
| 可测试性 | 能否独立验证 |
| 回滚成本 | 失败时是否易于撤销 |

优先选 **最简单且正确** 的方案，而非最「优雅」或最「通用」的。

## 4. 风险清单

动手前自问：

- 会不会引入 breaking change？
- 有没有竞态、死锁、资源泄漏风险？
- 是否需要迁移、配置变更或数据回填？
- 测试覆盖是否足够证明改动有效？

## 5. 执行顺序

1. 读代码 → 2. 定方案 → 3. 小步实现 → 4. 验证（遵循 general-engineering 验证要求）→ 5. 汇报结果

**禁止**：未读代码就大面积重写；未理解需求就猜测实现；跳过验证直接声称完成。
