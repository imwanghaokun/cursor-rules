---
description: 编码执行规范——最小改动、质量、验证、沟通（执行层）
alwaysApply: true
---

# 通用工程规范

复杂任务编码前先完成 `pre-coding-thinking` 中的思考；简单任务内化执行。

## 改动原则

- **最小改动**：只修改与任务直接相关的代码，不做无关重构、格式化或重命名
- **先读再写**：动手前阅读周边代码，匹配现有命名、包结构、抽象层次与注释风格
- **复用优先**：优先扩展已有函数/类/模块，禁止重复实现同类逻辑
- **不超前设计**：不为假设的未来需求抽象；两行能解决的逻辑不要抽 helper

## 代码质量

- 代码应自解释；注释只解释非显而易见的业务逻辑或深层技术细节
- 禁止空 catch、吞异常、无意义 TODO 占位
- 新增依赖须有必要性说明；不随意引入框架或工具库
- 未经明确要求，不主动创建 git commit / push / PR

## 验证要求

- 声称「完成 / 修复 / 通过」前，必须实际运行相关命令（编译、测试、lint）并确认输出
- 无法运行时，明确说明阻塞原因与已验证的部分，不得猜测结果

## 沟通与输出

- 用中文回复
- 回复篇幅与任务复杂度匹配；简单修复不堆砌长文
- 不创建用户未请求的文档（README、设计文档等）

## 规则优先级

1. 用户当次指令优先于一切规则
2. 过程层（pre-coding-thinking）> 执行层（本规则）> 结构层（architecture-standards）—— 仅当执行顺序有歧义时
3. 专题规则（performance、code-review）触发时叠加，不覆盖本规则基础约束
